{
    "name": "boodari.github.io",
    "version": "0.0.3",
    "type": "module",
    "scripts": {
        "build": "nuxt build",
        "dev": "nuxt dev",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare",
        "deploy": "nuxt generate && echo '' > .output/public/.nojekyll && gh-pages -d .output/public --dotfiles"
    },
    "dependencies": {
        "@nuxt/ui": "^4.4.0",
        "nuxt": "^4.3.0",
        "vue": "^3.5.27",
        "vue-router": "^4.6.4"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0"
    }
}
